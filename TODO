Things to be done:

 * handle errors: bad uri's, broken streams, d-bus problems etc.
 * improve GUI:
  - save window size and position
  - support simple command-line parameters
 * add command-line interface
 * add file accociations
 * support tags (also try to support legacy ID3 encodings mis-usage)
