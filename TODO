Things to be done:

 * handle errors: bad uri's, broken streams, d-bus problems etc.
 * improve GUI: support simple command-line parameters
 * add command-line interface
 * add file accociations
 * try to support legacy ID3 encodings mis-usage
 * add tests (check out the DBUS test runner thing)

 * implement and migrate to MPRIS specification instead of own interface
   (see http://wiki.xmms2.xmms.se/wiki/MPRIS)